package game.model.effects;

/**
 * enum class that propose value representing the effect need to select target in the map about their visibility from the shooter
 */
public enum TargetVisibility {
    VISIBLE,
    INVISIBLE,
    EVERYWHERE,
    DIRECTION
}
